# 使用示例

本文件展示 vibe-upskill 的典型使用场景和预期输出效果。

---

## 示例零：冷启动流程

### 场景
用户第一次使用本 Skill，没有任何 Agent 开发对话记录。

### AI 启动对话

```
AI：欢迎！这是你第一次使用 Vibe Upskill。

在开始之前，让我先了解你的情况：

1. 你目前处于哪个准备阶段？（基础积累 / 能力提升 / 查漏补缺）
2. 主要用什么技术栈？（如 Python/Django/React/K8s）
3. 这次主要是为了什么？（面试准备 / 技术能力提升 / 项目复盘沉淀）

另外——你目前有与 AI Agent 的开发对话记录吗？
```

```
用户：能力提升阶段，主要用 Python 后端和 React 前端，想准备面试。
暂时没有现成的对话记录。
```

```
AI：明白了。能力提升阶段，Python/React 开发者，目标是面试准备。

因为没有现成的对话记录，给你一个最小启动路径：

1. 【今天就可以做】下次用 Agent 开发时，在对话开头说"请在做关键决策时
   解释你的理由"——这会让 Agent 的推理过程更透明，后续复盘也更有料。
   
2. 【关注高价值时刻】特别留意 Agent 的这些信号：
   - "我选择 X 而不是 Y，因为..."
   - 排查 bug 时的逐步推理过程
   - 架构选型时的权衡讨论

3. 【设定首次复盘时间】建议你在 3-5 天内完成一次有意义的 Agent 开发后，
   回到这里，用 /vibe-upskill 触发复盘。

4. 【备选方案】如果你有纯手工完成的项目（没用 Agent），也可以现在直接
   描述给我——项目背景、关键决策、遇到的问题——我可以同样帮你生成面试题。

等你有对话记录了随时回来，我在这里。记住：宁缺毋滥，一条真实项目提炼
的题目胜过十道臆造题。
```

### 要点
- 不硬凑：没有对话记录时，诚实告知并提供替代路径
- 画像先行：即使没有对话，也先收集准备阶段和目标
- 给出具体可操作的下一个动作，而非模糊的"多练习"

---

## 示例一：从排错对话中提炼知识

### 输入（用户粘贴的 Agent 对话节选）

```
用户：我的 Docker 容器启动后立刻退出，日志里只看到 "exec: \"python\": executable file not found in $PATH"

Agent：这个错误说明容器内找不到 Python 解释器。让我检查你的 Dockerfile...
看到了，你用的是 `FROM alpine:3.18` 作为基础镜像，但没有安装 Python。
Alpine 是最小化镜像，默认不带任何运行时。

我来修复：
1. 将基础镜像改为 `FROM python:3.11-slim`
2. 或者在 Alpine 基础上添加 `RUN apk add --no-cache python3`

推荐方案 1，因为 python:slim 已经配置好了 PATH 和入口点。

用户：换了之后报新错：ModuleNotFoundError: No module named 'flask'

Agent：依赖没有安装进镜像。你的 Dockerfile 里 COPY 了代码但没有 RUN pip install。
需要在 COPY 之后加上：
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

另外建议用多阶段构建减小镜像体积...
```

### 模式一提取结果

```
【上下文】Docker 化一个 Python Flask 应用
【问题/决策】容器启动失败，找不到 Python 解释器
【Agent 的做法】
  1. 识别错误信息 → 容器内无 Python
  2. 检查 Dockerfile → 发现用了 alpine 基础镜像
  3. 给出两个方案，推荐 python:slim
  4. 修复后出现新错误 → 依赖未安装
  5. 添加 pip install 步骤，建议多阶段构建
【底层原理】
  - Alpine 是最小化 Linux 发行版，默认不含任何运行时
  - Docker 镜像分层构建，每一层需要显式安装所需软件
  - 多阶段构建可以分离编译环境和运行环境，减小最终镜像体积
【替代方案】
  - 方案 A：用 python:3.11-slim（推荐，开箱即用）
  - 方案 B：在 alpine 上 apk add python3（更小但需手动配置）
  - 方案 C：用 python:3.11 完整版（最大但兼容性最好）
【可迁移教训】
  - 选择基础镜像时，"最小"不等于"最好"，要权衡便利性和体积
  - 容器报错第一时间看日志，日志是排查的第一手信息
  - Docker 构建是声明式的，每个依赖都要显式声明
```

### 模式二生成的面试题

```
### [Bug 排查题] Docker 容器启动后立即退出

**难度**：基础
**标签**：#Docker #容器化 #排错
**来源**：基于 Flask 应用容器化的部署阶段提取

#### 场景描述
你刚把一个 Python Web 应用容器化，执行 docker run 后容器立刻退出了。
docker logs 显示 "executable file not found in $PATH"。

#### 问题
你的排查思路是什么？如何修复？

#### 参考思路
1. 首先看 docker logs 的具体错误信息
2. 分析 "executable not found" → 运行时环境缺失
3. 检查 Dockerfile 的 FROM 指令 → 基础镜像是否包含所需运行时
4. 修复后如果有新错误，继续迭代排查（依赖安装、端口映射等）

#### 反事实推理
- 如果这个应用是 Node.js 的，排查思路有什么不同？
- 如果你必须用 Alpine 镜像（公司安全策略），怎么处理？
- 如果容器不是立刻退出，而是运行 5 分钟后退出，排查方向怎么变？

#### 深挖追问
- Docker 镜像的分层机制是什么？为什么层数越少越好？
- 多阶段构建的原理是什么？它如何减小最终镜像体积？
- slim 镜像和 alpine 镜像的本质区别是什么？
```

### 模式三沉淀的避坑条目

```
### [坑-001] Alpine 基础镜像缺少运行时环境

**领域**：部署
**严重程度**：中
**来源**：Flask 应用容器化 - 2026-06-24

#### 现象
Docker 容器启动后立即退出，日志报 "executable file not found in $PATH"

#### 根本原因
Alpine 是最小化 Linux 发行版，默认不包含 Python/Node 等运行时。
开发者常因"镜像小"选择 Alpine，但忽略了它需要手动安装所有依赖。

#### 解决过程
1. 看日志 → 发现找不到 Python
2. 检查 Dockerfile → 发现 FROM alpine
3. 更换基础镜像为 python:slim → 解决
4. 出现新错误 → 依赖未安装 → 添加 pip install

#### 解决方法
将基础镜像从 alpine 换为对应运行时的 slim 版本（如 python:3.11-slim）

#### 预防措施
- 选择基础镜像时，先确认包含所需运行时
- 本地先 docker build && docker run 验证，不要直接推到 CI
- 在 Dockerfile 注释中标注选择该基础镜像的理由
```

---

## 示例二：从架构设计对话中提炼知识

### 输入（用户描述）

```
用户：我用 Agent 做了一个教育知识图谱项目，Agent 建议用 PostgreSQL 存图谱数据，
用 Neo4j 做可视化查询。我当时问为什么不都用 Neo4j，Agent 说因为关系型数据
还是用 PostgreSQL 更成熟，Neo4j 只做图查询。最后用了两个数据库，通过 API 层
同步数据。
```

### 模式二生成的面试题

```
### [方案对比题] 为什么用 PostgreSQL + Neo4j 双数据库架构？

**难度**：进阶
**标签**：#架构 #数据库 #知识图谱
**来源**：基于 MAD-EduKG 项目的架构设计阶段提取

#### 场景描述
你正在构建一个教育知识图谱系统，需要存储实体、关系，也需要做图遍历查询。
团队有人提议"全部用 Neo4j，一个数据库搞定"。

#### 问题
你会怎么分析这个提议？最终如何选择？

#### 参考思路
1. 分析数据访问模式：哪些是 CRUD 操作？哪些是图遍历？
2. 评估技术成熟度：团队对两种数据库的熟悉程度
3. 考虑运维成本：维护两个数据库 vs 一个数据库的复杂度
4. 设计数据同步策略：如果选双库，如何保证一致性？

#### 反事实推理
- 如果团队所有人都熟悉 Neo4j，还会选双库方案吗？
- 如果图查询只占 5% 的请求，双库的运维成本还值得吗？
- 如果用 PostgreSQL 的扩展（如 Apache AGE）做图查询，能否替代 Neo4j？

#### 深挖追问
- Neo4j 的 Cypher 查询语言和 SQL 的本质区别是什么？
- 两个数据库之间的数据同步，你用的什么策略？如何保证一致性？
- 如果同步延迟了，用户体验怎么保证？
```

---

## 示例三：完整的阶段性复盘对话流程

### AI 启动对话

```
AI：你好！我来帮你复盘这次与 Agent 的开发对话。

首先需要确认两件事：

1. 输入源：你打算怎么提供这次对话？
   - 粘贴对话记录（文本、截图、导出都可以）
   - 告诉我对话名称（比如"昨天排 Docker 那个对话"），我帮你在历史里找

2. 复盘范围：这次对话覆盖了哪些阶段？
   - 需求分析
   - 架构设计
   - 编码实现
   - 排错调试
   - 部署上线

建议每次只复盘一个阶段，这样提炼更深入。如果对话较短（< 50 轮），
可以选"全链路复盘"。
```

### 用户选择后，AI 进入模式一

```
AI：好的，我现在进入【拦截记录】阶段，从你提供的对话中提取关键知识点。

（提取完成后）

我提取了 5 个知识点，其中 2 个推断理由标注了 [待确认]：

1. [架构] 选择 Redis 做会话缓存 — [待确认] 推断理由是"高频读取 + 简单 KV 结构"
2. [排错] ORM 字段与数据库不同步导致 500 错误
3. [部署] 用 docker-compose 编排多服务 — [待确认] 推断理由是"本地开发环境一致性"
4. [编码] 用 React Context 做全局状态管理
5. [方法论] Agent 先写接口定义再实现的路径

请确认标注 [待确认] 的推断是否准确，或者告诉我实际的理由。
```

### 确认后进入模式二

```
AI：确认完毕，现在进入【面试题生成】阶段。

基于 5 个知识点，我生成了 8 道面试题。根据你的准备阶段（能力提升），
难度分配为：基础 1 道、进阶 4 道、高级 3 道。

- 场景设计题 × 1（进阶）
- Bug 排查题 × 2（基础 × 1、进阶 × 1）
- 深挖原理题 × 2（进阶 × 1、高级 × 1）
- 方案对比题 × 1（进阶）
- 项目表达题 × 1（高级）
- 复盘反思题 × 1（高级）

现在请你看第一题（基础·Bug 排查题），然后用自己的话口述回答思路。
不要看参考答案，卡住的地方标记一下。

---

（用户复述第一题后）

AI：好的，请自评一下掌握度：
  0 — 完全卡住，基本没想起来
  1 — 部分回忆，大致方向对了但有遗漏
  2 — 流畅复述，能完整讲清思路和原理

用户：1 分吧，思路说对了但忘了具体的 Docker 命令细节。

AI：了解了。【掌握度 1 — 部分回忆】【诊断：细节遗忘】
下次复习安排在明天（间隔 1 天）。

这道题标记为"复习中"。卡在 Docker 命令细节上，建议快速回顾一下
`docker logs`、`docker inspect` 几个常用排查命令的用法就可以。

继续下一题？

---

（用户连续跳过 3 道题后）

AI：提醒一下——你已经跳过 3 道题了（Q-002 场景设计、Q-004 深挖原理、
Q-006 项目表达）。要不要回头挑一道最简单的试试？Q-002 只需要 2 分钟，
你觉得呢？

---

（用户又跳过了几道，达到 6 道）

AI：连续跳过 6 道题——这些题是基于你真实项目生成的，不练的话和你的
项目经验就脱节了。建议至少复述 1 道再继续。要不要试一下 Q-003 
Bug 排查题？这道题跟 Docker 相关，你刚才第一道掌握得还可以。

---

（用户坚持跳过，达到 10 道）

AI：本会话已跳过 10 道题。剩下的题目将保存到知识库中标记为 [未复述]，
在复习提醒中优先出现。

建议你现在选一道复述，或我们结束本次复盘，下次再继续？

用户：好，我做 Q-005 深挖原理题吧。
```

### 最后进入模式三

```
AI：所有题目过了一遍，现在进入【知识蒸馏】阶段。

我将把本次复盘结果追加到你的知识库：
- interview-qa.md：新增 8 道题
- pitfall-checklist.md：新增 2 条避坑条目
- review-schedule.md：生成复习调度表（1 道明日到期，7 道待首次复述）
- knowledge-index.md：更新统计数字

检测到 interview-qa.md 中已有一道类似的 Bug 排查题（Q-012），
我将新题合并进去，增加了"ORM 同步"这个角度。

知识库已更新。本次复盘完成！

📋 复习提醒：
  明天有 1 道题到期复习：Q-001（Redis 缓存策略 — 上次评分 1）
  7 道题标记为 [未复述]，在复习提醒中优先出现

建议下一步：
1. 明天复习 Q-001，争取评分从 1 提升到 2
2. 对 Redis 内存淘汰机制做一个专项学习（你今天卡住的知识点）
3. 下次部署阶段复盘时，重点关注 CI/CD 相关的知识点
4. 尽快回来把 7 道未复述题过一遍——按间隔算法，首轮复习越早越好
```

---

## 示例四：安装与跨平台使用

### 最简安装（一句话）

直接把这句话发给你的 AI Agent：

```
帮我安装这个 Skill：https://github.com/bb-cccc/vibe-upskill
```

Agent 会自动完成克隆、放置、验证全流程。适用于 Claude Code、Cursor、Copilot 等任何支持 Skill/插件机制的 Agent。

### 手动安装

本 Skill 设计为通用格式，可在多种 AI Agent 中使用：

**Claude Code**：将 `vibe-upskill/` 目录放在项目的 `.claude/skills/` 下，Claude Code 会自动识别 SKILL.md 并在合适时机调用。

**Codex / 其他 Agent**：将 SKILL.md 放在项目根目录或 `.agent/skills/` 下，在对话中提及"复盘"、"面试题生成"等触发词即可激活。

**通用用法**：在任何 AI Agent 对话中，直接粘贴 SKILL.md 的内容作为系统指令，然后提供你的开发对话记录即可开始复盘。

---

## 示例五：间隔复习流程

### 场景
用户三天前完成了一次复盘，今天重新启动 Skill。系统检测到有到期的复习任务。

### AI 交互流程

```
AI：欢迎回来！

📋 复习检查中...
  review-schedule.md 已加载

今日到期复习：3 道题
🟡 Q-001 Redis 缓存策略 — 上次评分 1 — 间隔 3 天
🟡 Q-003 Docker 容器排查 — 上次评分 1 — 间隔 1 天
🟡 Q-007 PostgreSQL vs MongoDB — 上次评分 0 — 间隔 1 天

另有 7 道题标记为 [未复述]。

今天先处理到期复习（3 道，在合理范围内），还是你想开始一次新的复盘？
```

```
用户：先复习吧。
```

```
AI：好，从 Q-003 开始（上次评分最低的一道已复习过，先看 Q-001）。
请看题：

### [深挖原理题] Redis 缓存策略选型

（展示题目，不展示参考答案）

请用自己的话口述回答。
```

```
用户：（复述结束后）这次思路清晰多了，Redis 的淘汰策略我专门查过，
LRU 和 LFU 的区别能说清楚，过期删除的惰性+定期策略也记住了。给自己评 2 分。
```

```
AI：很好！评分 2 — 流畅复述。
间隔更新：1天 → 3天，下次复习日期：6月27日。

Q-003 和 Q-007 继续？
```

```
用户：继续。Q-003 还可以，Q-007 还是有点卡。
```

```
AI：Q-003 — 评分 2，间隔 1天 → 3天，下次 6月27日。
Q-007 — 评分 1，间隔保持 1天，下次 6月25日。
     【诊断】你说对了一部分，但 Postgres 的 MVCC 和 MongoDB 的
     WiredTiger 区别没说清楚。建议查一下这两个概念。
     明天再来一次，争取提升到 2 分。

📊 本次复习总结：
  - 已复习 3 道 | 提升 2 道 | 仍需努力 1 道
  - 明日到期：1 道（Q-007）
  - 7 道 [未复述] 仍在队列中，建议尽快安排

下次复习提醒：
  - 明天（6/25）：Q-007
  - 后天（6/27）：Q-001, Q-003
```

### 要点
- 启动时自动检查 review-schedule.md，展示到期任务
- 复习时不展示参考答案，复述后才揭晓
- 每次复述后即时评分并更新间隔
- 给出下次复习的具体日期，而非模糊的"过几天再复习"
- 特别关注评分为 0 的题目——这些是真正的薄弱点

---

## 示例六：季度归档审查

### 场景
用户使用 Skill 已超过 90 天，系统触发首次季度审查。

### AI 交互流程

```
AI：📦 季度知识库审查

你已使用 Vibe Upskill 超过 90 天，知识库中有 23 个条目需要审查。

以下是超过 90 天未审查的条目：

超过 180 天（3 条）——建议优先处理：
  - Q-002 Angular 组件通信 — 2026-01-10 — 技术栈：Angular 15
  - 坑-003 Webpack 配置优化 — 2025-12-20 — 技术栈：Webpack 4
  - KB-005 jQuery AJAX 全局配置 — 2025-11-15 — 技术栈：jQuery 3.6

超过 90 天（20 条）——逐条快速确认即可。

先处理超过 180 天的 3 条，每条请回复：
  [仍有效] / [已过时] / [已替代·新方案是___] / [稍后审查]
```

```
用户：Q-002 已过时，我们团队已经不用 Angular 了。
      坑-003 已替代，现在用 Vite。
      KB-005 仍有效，jQuery 老项目维护时还会用到。
```

```
AI：已处理：
  - Q-002 → 移入 archive/（已过时：Angular 不再使用）
  - 坑-003 → 移入 archive/（已替代：Vite 取代 Webpack）
  - KB-005 → 更新审查日期为 2026-06-24，标记 [仍有效]

继续审查剩下 20 条？还是分批处理？
```

### 要点
- 首次季度审查自动触发，不依赖用户记住
- 优先处理最久的条目（超过 180 天）
- 归档不是删除——移入 archive/ 保留历史参考
- "仍有效"的条目只需更新审查日期，不改变其他状态

---

## 示例七：种子对话体验（新用户立即上手）

### 场景
用户第一次使用 Skill，没有任何 Agent 开发对话记录。选择体验种子对话。

### AI 交互流程

```
用户：使用种子对话体验一下。

AI：好的！我将使用内置的示例对话（18 轮，覆盖 Docker 排错 + 数据库选型 +
Redis 缓存策略 + 架构演进）为你演示完整的三模式流程。

首先确认两件事：

1. 你目前处于哪个准备阶段？（基础积累 / 能力提升 / 查漏补缺）
2. 主要用什么技术栈？

用户：能力提升，Python 后端。

AI：好的。现在进入【模式一：拦截记录】——

（扫描种子对话...）

从这段对话中我提取了 5 个知识点：

1. [部署] Alpine 基础镜像缺少运行时 → Docker 镜像选型原则
2. [架构] PostgreSQL vs MongoDB 选型 → 关系型 vs 文档型数据库
3. [排错] Docker 容器多阶段构建 → 减小镜像体积
4. [编码] Redis 缓存策略与缓存穿透/雪崩防护
5. [方法论] "记模式不记细节"——判断力 > 记忆力

进入【模式二：面试题生成】——

基于 5 个知识点 + 能力提升阶段，生成 8 道题：
- Bug 排查题 × 2（基础 × 1、进阶 × 1）
- 深挖原理题 × 2（进阶 × 1、高级 × 1）
- 方案对比题 × 1（进阶）
- 系统设计题 × 1（高级）
- 项目表达题 × 1（进阶）
- 复盘反思题 × 1（高级）

现在请你看第一题（基础·Bug 排查题），用 30 秒说出核心思路。
```

### 要点
- 种子对话让新用户立即体验完整流程，无需等待积累真实对话
- 体验后用户清楚知道"下次我用自己的对话时会发生什么"
- 种子对话覆盖多个高价值技术场景，生成题目质量可直观感知

---

## 示例八：分层复述与即刻补刀

### 场景
用户在做一道深挖原理题，骨架复述通过但完整复述卡在细节上。

### AI 交互流程

```
AI：请看这道题——

### [深挖原理题] PostgreSQL 的 MVCC 机制是怎么工作的？

请先用 30 秒说出核心思路：MVCC 解决什么问题？靠什么机制实现？

用户：MVCC 就是多版本并发控制。PostgreSQL 里 UPDATE 不会直接覆盖旧数据，
而是插入新版本。不同事务看到的是不同快照。这样读操作不会被写操作阻塞。

AI：核心思路正确！【骨架复述 ✓】

要不要试试完整复述？补充具体的实现细节——元组版本链、事务快照、
VACUUM 的作用、和 MySQL MVCC 的区别。

用户：好... 元组版本链我记不太清了。PostgreSQL 每行有 xmin 和 xmax
表示版本的可见范围... 然后... VACUUM 是干什么来着？卡住了。

AI：哪里卡住了？标注一下性质：
  [概念不清] / [逻辑链断裂] / [细节遗忘]

用户：细节遗忘。VACUUM 的作用和具体命令忘了。

AI：【即刻补刀 — 速查卡】

VACUUM 相关关键点：
- `VACUUM` — 标记死元组（dead tuples）为可复用空间，不锁表
- `VACUUM FULL` — 物理压缩表空间，但会锁表（生产慎用）
- `ANALYZE` — 更新统计信息，帮助查询计划器做正确决策
- `autovacuum` — PostgreSQL 默认开启的后台进程，自动清理死元组
- 为什么需要 VACUUM：PostgreSQL 的 UPDATE = INSERT + MARK OLD，
  旧版本不会自动删除，积累多了会膨胀表和索引，拖慢查询

现在试试看——用 30 秒重新说一遍完整的 MVCC 机制？

用户：PostgreSQL 的 MVCC 通过元组版本链实现。每行数据有 xmin（插入时
事务 ID）和 xmax（删除时事务 ID）。UPDATE 操作实际是插入新版本并标记
旧版本。不同事务通过快照看到不同版本——只能看到 xmin < 当前事务 ID
且 xmax > 当前事务 ID 的行。旧版本由 autovacuum 后台进程定期清理，
否则表会膨胀。MySQL 的 MVCC 通过 undo log 实现，和 PostgreSQL 不一样。

AI：这次流畅多了！【完整复述 ✓】【评分：2 — 接近掌握】
补刀后从卡住到基本完整，xmin/xmax 和 VACUUM 都说清楚了。
评分 2，间隔 3 天。下次复习时期待你冲到评分 3。

【补刀记录】
- 诊断：细节遗忘
- 补刀类型：速查卡（VACUUM 5 个关键点）
- 补刀后评分：0 → 2（跨级提升——细节补上后骨架本就坚实）
```

### 要点
- 分层复述：骨架先过→有信心→更容易接受完整复述挑战
- 即刻补刀：当场补上缺口，用户不用"带着困惑等明天"
- 补刀可能跨级提分（不只是 0→1，骨架本就坚实时可直接到 2）

---

## 示例九：快问快答（5 分钟完整闭环）

### 场景
用户刚跟 Agent 做完一个技术决策，趁热打铁想搞懂。

### AI 交互流程

```
用户：快问快答：为什么 Agent 选了 Redis Cluster 而不是 Codis？

AI：【模式四·快问快答】好，我们 5 分钟搞定这个决策。

**第一步：追问到底**

先确认背景——你这个项目里 Redis 的典型使用场景是什么？

用户：做分布式会话缓存和限流计数器，日均请求量大概 500 万。

AI：了解。那 Agent 给的理由是什么？你还记得当时怎么讨论的吗？

用户：Agent 说 Redis Cluster 是官方方案，Codis 是第三方中间件，
维护成本更高。但我想知道本质区别——为什么官方方案就更可靠？

AI：好问题。那我们追问到底——

层级 1（做了什么）：Agent 选了 Redis Cluster 而不是 Codis。
层级 2（为什么）：Redis Cluster 是官方原生方案，去中心化架构，
  每个节点独立负责一部分 slot；Codis 是豌豆荚开源的代理层方案，
  需要一个 Centralized Proxy 做路由。
层级 3（凭什么）：去中心化 vs 代理模式的核心权衡——
  - Redis Cluster：客户端直接算 slot → 节点，无代理层，少一跳延迟，
    但客户端需要支持集群协议（有 SDK 要求）
  - Codis：客户端连 Proxy 就行（兼容单机 Redis 协议），但 Proxy 本身
    是单点瓶颈 + 额外一跳延迟

现在生成你的面试题——

### [方案对比题] Redis Cluster vs Codis：分布式缓存方案选型

（完整的场景描述、答案要点、反事实推理...）

**第二步：骨架复述**

请用 30 秒说出：Redis Cluster 和 Codis 的本质区别是什么？选型时
你最看重哪个因素？

用户：本质区别是架构模式——Cluster 是去中心化的，客户端直接算 hash
slot 定位节点，Codis 是中心化代理模式，所有请求先到 Proxy 再转发。
选型时最看重运维复杂度和客户端兼容性——如果团队已有的 Redis 客户端
不支持 Cluster 协议，Codis 可能更现实。

AI：【骨架复述 ✓】【评分：1 — 骨架通过】

核心判断对了。要不要花 2 分钟做完整复述？补充 slot 分配机制的具体
细节、故障转移流程、水平扩容怎么操作？

用户：今天时间紧，先跳过完整复述。

AI：没问题。这道题已存入知识库，标记 [仅骨架]，间隔 1 天。
下次启动 Skill 时会提醒你复习。明天花 2 分钟补上细节就行。

⏱️ 本次快问快答：4 分 30 秒。下线！
```

### 要点
- 5 分钟完成追问→出题→复述闭环，适合碎片化学习
- 没有完整对话也能触发，降低使用门槛
- 骨架复述必做，"今天没时间"至少保底一个核心思路

---

## 示例十：知识依赖拓扑复习

### 场景
用户启动 Skill 准备复习。系统检测到今日到期 4 道题，按拓扑层级排序后展示。

### AI 交互流程

```
AI：欢迎回来！📋 复习检查中...

今日到期复习：4 道题。已按依赖拓扑排序（先复习基础知识，再攻上层）：

🔴 层级 0（基础知识，被 3 道题依赖）：
  KB-001 数据库事务 ACID — 上次评分 1 — 间隔 1 天

🟡 层级 1（依赖 KB-001）：
  Q-003 PostgreSQL MVCC 原理 — 上次评分 1 — 间隔 3 天

🟢 层级 2（依赖 Q-003 + KB-002）：
  Q-007 PostgreSQL vs MongoDB — 上次评分 0 — 间隔 1 天
  Q-012 慢查询 EXPLAIN 分析 — 上次评分 2 — 间隔 7 天

推荐顺序：KB-001 → Q-003 → Q-007 / Q-012

⚠️ 注意：Q-007 依赖 KB-001 和 Q-003——如果这两道没复习好，
Q-007 大概率还会卡住。建议按推荐顺序来。

开始复习 KB-001？

用户：好。

---

（KB-001 复习完成后...）

AI：KB-001 评分为 3 — 流畅复述！间隔：1天 → 7天。

🔗 依赖链连锁更新：
  - Q-003 PostgreSQL MVCC（依赖 KB-001）→ 前置已就绪 ✓
  - Q-007 PostgreSQL vs MongoDB（依赖 KB-001 + Q-003）
    → KB-001 ✓，Q-003 仍待复习

继续 Q-003？

用户：继续。

---

（Q-003 也复习完成，评分 2）

AI：Q-003 评分 2 — 接近掌握。间隔：3天 → 7天（温和延长）。

🔗 依赖链更新：
  - Q-007 PostgreSQL vs MongoDB 的两个前置知识现在都已就绪 ✅
  - 建议趁热打铁完成 Q-007——前置障碍清除了，这道题你今天应该能答得比上次好很多。

用户：好，来。

---

（Q-007 复习完成，评分从 0 跳到了 2）

AI：Q-007 评分从 0 → 2！这就是拓扑复习的效果——上次你卡住，
不是因为这道题本身难，而是因为 KB-001 和 Q-003 没掌握。
现在前置知识到位了，这道题自然就通了。

📊 本次复习总结：
  - 已复习 4 道 | 提升 3 道 | 评分跳级 1 道（Q-007: 0→2）
  - 拓扑复习效果：Q-007 的提分完全归功于前置知识的巩固

下次复习提醒：
  - 明天：无到期
  - 6/30：Q-003, KB-001
  - 7/1：Q-012
```

### 要点
- 拓扑排序让复习从"头痛医头"变成"从根上解决问题"
- 依赖链连锁更新让用户看到"前置掌握后，后续题目自动变简单"
- 评分跳级（0→2）是拓扑复习的典型信号——不是题目变简单了，是你真的懂了
